//Lytskaya Alisa NKAbd-03-24 Линейная структура С++
#include <iostream>
#include <cmath>
using namespace std;
/*Задача № 4 на С++: Считая, что Земля – идеальная сфера с радиусом r=6350 км, определить расстояние до линии горизонта от точки с заданной высотой над Землей.*/
int main() {
	float d, h, r;
	cout<<"Программа для вычисления растояния до линии горизонта по данному  h"<<endl;
	cout<<"Введите h "<<endl;
	cin>>h;
	r=6350;
	/*для вычисления расстояния до линии горизонта необходимо использовать теорму Пифогора(это прямоугольный треугольник, потому что радиус перпендикулярен касательной, а касательной в данной задаче выступает прямая проведенная от точки с заданной высотой к Земле), тк растояние от центра земли это гипотенуза= r+h, r- первый катит, d- второй, следовательно (r+h)^2=r^2+d^2, следовательно d=sqrt((r+h)^2-r^2)=sqrt(2 * r * h + h * h)*/
	d = sqrt(2 * r * h + h * h); 
	cout << "Расстояние до линии горизонта: " << d << " км" << endl;

}
